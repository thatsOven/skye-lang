macro N 10;

fn main() !void {
    let a = 0;
    let b = 1;

    let stringBuffer = core::MutableString::new();
    defer stringBuffer.free();

    for let i = 0; i < @N; i++ {
        use core::utils::intToString as intToStr;
        
        intToStr(&stringBuffer, a);
        defer stringBuffer.clear();

        println(stringBuffer.toString());
        
        const c = a + b;
        a = b;
        b = c;
    }

    return (!void)::Ok;
}

