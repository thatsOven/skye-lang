namespace std {
    namespace math {
        fn max[T](a: T, b: T) T {
            return a > b ? a : b;
        }

        fn min[T](a: T, b: T) T {
            return a < b ? a : b;
        }
    }
}